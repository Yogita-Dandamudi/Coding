        ls=[]
        for i in range(0,numRows):
            ls.append([0]*(i+1))
            for j in range(len(ls)):
                ls[i][j]=factorial(i)//(factorial(j)*factorial(i-j))
        return ls
